#!/usr/bin/env sh
APP_BASE_NAME=`basename "$0"`
DIRNAME=`dirname "$0"`
CLASSPATH=$DIRNAME/gradle/wrapper/gradle-wrapper.jar
JAVA_EXE=java
exec "$JAVA_EXE" -Xmx64m -cp "$CLASSPATH" org.gradle.wrapper.GradleWrapperMain "$@"
